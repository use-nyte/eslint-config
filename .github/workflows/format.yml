name: Prettier

on:
    pull_request:
        branches:
            - main

permissions:
    contents: write

jobs:
    format:
        name: Format Code
        uses: use-nyte/prettier-config/.github/workflows/format.yml@main
        secrets: inherit